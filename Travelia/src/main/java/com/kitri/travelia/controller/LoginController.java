package com.kitri.travelia.controller;

import org.springframework.stereotype.Controller;

@Controller
public class LoginController {
	/*
	@RequestMapping(value="login.html", method=RequestMethod.POST)
	public String login(Member memeber, MemberDAO memberDAO, HttpSession session){
		Member member = MemberDAO.getMember(member);
		if(member!=null){
			session.setAttribute("memberName", member.getNick() );
			return "board.html";
		}
		else return "main.html";
	}
	*/

}
